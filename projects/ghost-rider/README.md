# Ghost Rider Fan Archive

This project now lives inside the portfolio repository.

- Editable React source: `src/`
- Public portfolio route: `/ghost-rider/`
- Production-ready static export: `../../public/ghost-rider/`

The static export is intentionally committed with the portfolio so its live
experience does not depend on a separate repository or deployment.
